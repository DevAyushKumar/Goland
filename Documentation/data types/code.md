Data types:
-> In any programming languages you have multiple data types 
-> Difference: which data types do they support exactly?
--> Strings, Integers, Booleans, Maps, Array,..

Basic data types:
String:
-> For textual data, defined with double quotes, e.g, "this is a string"
Integers:
Integers:
-> Representing whole numbers, positive and negative, e.g 5,120,etc.

Different use cases of data types:
-> Strings: "AB" + "CD"
-> Integers: 5+10
-> Each data type can do different things and behave differently 

Go is statically typed language:
-> you need to tell Go complier, the data type when declaring the variable
-> Type interface: But, Go can infer the type when you assign a value

More Robust, reuces the likelihood of error
Helps developer to catch type mismatches sooner(at compile time)
Discovers mistake at compile time, not at runtime

Integer types:
-> Whole numbers

Floating types:
-> Numbers that contains a decimal component (real numbers)

Bit size:
Convenience to choose the best suits your memory needs (or memory strictness)
signed/unsigned:

Signed/Unsigned:
Eg: make sure only, positive numbers are accepted for some data 

Floating point data:
-> Numbers that contain a decimal component 

"Syntactic Sugar" in programming:
-> A term to describe a feature in a language that let's you do smt more easily.