variable:
-> Variables are used to store values
-> Like containers for values 
-> Give the variable a name and referenace everywhere in the app
Benefits:
-> Update the value only once 
make our app more flexible 

Variables names should be descriptive

Go complie errors to enfore better code quality:
-> Varibale name must be used
-> To avoid possible dead code, code that is never used in the code.
-> Imported packages must be used

Constants:
-> Constants are like variables, except that their values cannot be changed.

Print formatted date:
fmt.Println("Some text with a variable %s", my variable)
-> It takes a template string that contains the text that needs to be formatted.
-> Plus some annotations verbs (or placeholder) that tells the fmt functions how to format the variables passed in.
